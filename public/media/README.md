# Media assets for landing page

Add your media files here with these exact names:

- `dribbling.webm` (preferred background video)
- `dribbling.mp4` (fallback video)
- `dribbling-poster.jpg` (fallback image before video loads)
- `muje.mp3` (music track used by the Play/Pause button)

These files are referenced by `src/App.jsx` via `/media/...` URLs.
